#include <iostream>
using namespace std;

vector<pair<int, int>> flower; // st, en
int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n;
	int days[] = {0, 0, 0, 31, 30, 31, 30, 31, 31, 30, 31, 30, 0};
	for (int i = 0; i < n; ++i) {
		int sm, sd, em, ed;
		cin >> sm >> sd >> em >> ed;
		if (days[sm] 
	}
}
